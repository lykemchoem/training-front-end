handleNames = () => {
    let fullName = document.getElementById('full-name').value.trim();
    let arrFullName = fullName.split(' ');
    
    // số từ trong chuỗi
    

    document.getElementById('now-chain').value = nowchain;
}